---
phase: 09-type-safety
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - scripts/services/TokenService.js
  - scripts/services/TVACacheService.js
  - scripts/services/IndexService.js
  - scripts/services/SearchOrchestrator.js
  - scripts/services/StorageService.js
  - scripts/services/ScanService.js
  - scripts/services/ForgeBazaarService.js
  - scripts/services/SearchService.js
  - scripts/core/Utils.js
  - scripts/main.js
  - scripts/ui/UIManager.js
  - scripts/workers/IndexWorker.js
  - package.json
autonomous: true
requirements:
  - TYPE-02
  - TYPE-03

must_haves:
  truths:
    - "All public methods on 5 required services have @param and @returns JSDoc"
    - "tsc --noEmit reports zero errors"
    - "npm run typecheck exits with code 0 (|| true removed)"
    - "All existing tests still pass"
  artifacts:
    - path: "scripts/services/TokenService.js"
      provides: "Annotated public methods + D&D 5e system casts"
      contains: "@param"
    - path: "scripts/services/TVACacheService.js"
      provides: "Annotated public methods + Promise return fixes"
      contains: "@returns"
    - path: "scripts/services/IndexService.js"
      provides: "Annotated public methods"
      contains: "@param"
    - path: "scripts/services/SearchOrchestrator.js"
      provides: "Annotated public methods + Fuse constructor fix"
      contains: "@param"
    - path: "scripts/services/StorageService.js"
      provides: "Annotated public methods + EventTarget.result cast"
      contains: "@param"
    - path: "package.json"
      provides: "typecheck script without || true"
      contains: "tsc --noEmit --project jsconfig.json\""
  key_links:
    - from: "scripts/types/typedefs.js"
      to: "scripts/services/TokenService.js"
      via: "import type reference in @returns annotation"
      pattern: "import.*typedefs"
    - from: "package.json"
      to: "npm run typecheck"
      via: "script exits non-zero on type errors"
      pattern: "tsc --noEmit"
---

<objective>
Add JSDoc @param/@returns annotations to all public service methods, fix all remaining tsc errors across the codebase, and make `npm run typecheck` a real quality gate (remove `|| true`).

Purpose: This brings tsc --noEmit to zero errors, making type checking an actual CI gate that catches regressions.
Output: Annotated service files, zero tsc errors, typecheck script as real gate
</objective>

<execution_context>
@/home/aiacos/.claude/get-shit-done/workflows/execute-plan.md
@/home/aiacos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-type-safety/09-RESEARCH.md
@.planning/phases/09-type-safety/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate service methods and fix service file type errors</name>
  <files>scripts/services/TokenService.js, scripts/services/TVACacheService.js, scripts/services/IndexService.js, scripts/services/SearchOrchestrator.js, scripts/services/StorageService.js, scripts/services/ScanService.js, scripts/services/ForgeBazaarService.js, scripts/services/SearchService.js</files>
  <action>
For each of the 5 required service files (TokenService, TVACacheService, IndexService, SearchOrchestrator, StorageService) plus ScanService, ForgeBazaarService, and SearchService:

1. **Add `@param` and `@returns` JSDoc to ALL public methods.** Use typedef imports where applicable: `@returns {import('../types/typedefs.js').CreatureInfo}`. Every public method (not prefixed with `_`) must have complete JSDoc annotations.

2. **Fix file-specific type errors using strategies from research:**

**TokenService.js (12 errors):**
- D&D 5e `actor.system.details` errors: Cast `actor.system` to `any` with `/** @type {any} */` at access points (Pattern 5 from research). This module is D&D 5e-only.
- `texture.src` error: Cast with `/** @type {any} */`
- `toLowerCase on never`: Follows from the details fix
- `'texture.src'` and `'prototypeToken.texture.src'` in object literals (lines 205, 212): These are dot-notation property paths for Foundry's update system. Use `@ts-expect-error` with comment explaining Foundry update path syntax.

**TVACacheService.js (4 errors):**
- 3 async Promise return type errors (lines 418, 466, 529): Change `@returns {any[]}` to `@returns {Promise<any[]>}` (Pattern 6)
- `.api` error (line 33): Should be resolved by Plan 01's modules.d.ts. If still present, verify modules.d.ts is correct.

**IndexService.js (3 errors):**
- `.api` error (line 57): Should be resolved by Plan 01. If still present, verify.
- `window.TVA` errors (lines 628-629): Should be resolved by Plan 01's globals.d.ts. If still present, verify.

**SearchOrchestrator.js (1 error):**
- `new this._FuseClass()` not constructable (line 298): Use `@ts-expect-error` with comment "Fuse constructor injected via DI, type cannot express constructor constraint in JSDoc"

**StorageService.js (1 error):**
- `event.target.result` (line 99): Cast `event.target` to `IDBRequest` with `/** @type {IDBRequest} */` (Pattern from research)

**ScanService.js (3 errors):**
- Setting "not assignable to core" (line 26): Should be resolved by Plan 01. If still present, verify.
- `.split` on Setting (line 29): Cast setting value with `/** @type {string} */`
- `.api` error (line 211): Should be resolved by Plan 01.

**ForgeBazaarService.js (1 error):**
- `game.forge` (line 61): Use `@ts-expect-error` with comment "Forge VTT platform extension — not typed by fvtt-types"

**SearchService.js:** Add `@param`/`@returns` annotations to public methods. No known type errors.

After fixing all service files, run: `npx tsc --noEmit --project jsconfig.json 2>&1 | grep "scripts/services/" | wc -l` — should be 0.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project jsconfig.json 2>&1 | grep "scripts/services/" | wc -l</automated>
  </verify>
  <done>All 8 service files have zero tsc errors. All public methods have @param and @returns JSDoc annotations.</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining non-service errors and enable typecheck gate</name>
  <files>scripts/core/Utils.js, scripts/main.js, scripts/ui/UIManager.js, scripts/workers/IndexWorker.js, package.json</files>
  <action>
Fix all remaining tsc errors in non-service files to reach zero total errors.

**scripts/ui/UIManager.js (51 errors):**
Add `// @ts-nocheck` as the FIRST line of the file. UIManager has 51 DOM type narrowing errors (Element vs HTMLElement). Per research recommendation, this is pragmatic — UIManager is not a service file and full DOM typing provides no immediate value for this phase.

**scripts/workers/IndexWorker.js (1 error):**
Add `@ts-expect-error` on the `new Function()` line (line 308) with comment "Dynamic function construction for performance optimization in Worker scope".

**scripts/main.js (14 errors):**
- Settings errors (lines 124, 157, 167, 181, 190, 199, 208, 217, 226, 235, 250): Should be resolved by Plan 01's settings.d.ts. If still present, verify settings.d.ts has correct entries.
- `.api` error (line 149): Should be resolved by Plan 01's modules.d.ts.
- `window.TokenReplacerFA` (line 757): Should be resolved by Plan 01's globals.d.ts.
- Hook registration error (line 907, TS2769 "No overload matches this call"): This is a fvtt-types gap — `getSceneControlButtons` hook may not be typed. Use `@ts-expect-error` with comment "fvtt-types beta does not include getSceneControlButtons hook type".

**scripts/core/Utils.js (6 errors):**
- `window.Fuse` errors (lines 40-41): Should be resolved by Plan 01's globals.d.ts.
- `TemplateDelegate` return type errors (lines 438, 440): These are `loadTemplates()` return value issues. Use `@ts-expect-error` with comment explaining Foundry's loadTemplates returns void per API design.
- Settings error (line 471): Should be resolved by Plan 01's settings.d.ts.
- String argument to settings (line 487): Cast with `/** @type {any} */` or use `@ts-expect-error` — this is a dynamic key passed to `game.settings.get()`.

**package.json:**
Change the typecheck script from:
```
"typecheck": "tsc --noEmit --project jsconfig.json || true"
```
to:
```
"typecheck": "tsc --noEmit --project jsconfig.json"
```
This makes `npm run typecheck` exit non-zero on type errors, enabling CI to catch regressions.

**IMPORTANT:** Only remove `|| true` AFTER confirming zero errors. Run `npx tsc --noEmit --project jsconfig.json` first to verify zero errors, THEN update package.json.

After all changes: `npm run typecheck` must exit 0, `npx vitest run` must still pass.
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run --passWithNoTests</automated>
  </verify>
  <done>tsc --noEmit reports zero errors. npm run typecheck exits 0 without || true. All existing tests pass. CI typecheck is now a real gate.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npm run typecheck` exits 0 (zero tsc errors, no || true)
- `npx vitest run` passes (no regression)
- `npx tsc --noEmit --project jsconfig.json 2>&1 | grep "error TS" | wc -l` shows 0
- All 5 required service files have @param/@returns on public methods
</verification>

<success_criteria>
- All public methods on SearchOrchestrator, TokenService, TVACacheService, IndexService, StorageService have @param and @returns JSDoc
- tsc --noEmit reports zero errors across the entire codebase
- npm run typecheck is a real quality gate (|| true removed)
- All existing Vitest tests pass unchanged
- UIManager.js uses @ts-nocheck (pragmatic, out of service scope)
</success_criteria>

<output>
After completion, create `.planning/phases/09-type-safety/09-02-SUMMARY.md`
</output>
